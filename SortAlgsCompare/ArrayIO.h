#ifndef ARRAY_IO
#define ARRAY_IO

#include <stdio.h>
#include <stdlib.h>
#include <iostream>

int* randArray(int, int, int);
void printArray(const int*, int);
int* orderedArray(int);
int* revOrderedArray(int);

#endif // !ARRAY_IO
